---
const { tag, href = `/tags/${encodeURIComponent(tag)}` } = Astro.props as { tag: string; href?: string }
---

<a
  href={href}
  class='rounded-full border border-primary/30 bg-primary/5 px-3 py-1 text-xs font-medium text-primary transition hover:bg-primary/10'
>
  {tag}
</a>
